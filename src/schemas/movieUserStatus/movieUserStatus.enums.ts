export enum MovieUserStatusFavorites {
  NotFavorite = 0,
  Favorite = 1,
}

export enum MovieUserStatusWatch {
  NotWatch = 0,
  Watch = 1,
}

export enum MovieUserStatusWatched {
  NotWatched = 0,
  Watched = 1,
}

export enum MovieUserStatusBuy {
  NotBuy = 0,
  Buy = 1,
}

export enum MovieUserStatusRatingStatus {
  NotAvailable = 0,
  Available = 1,
}

export enum MovieUserStatusStatus {
  InActive = 0,
  Active = 1,
  Deactive = 2,
}

export enum MovieUserStatusDeletionStatus {
  NotDeleted = 0,
  Deleted = 1,
}
