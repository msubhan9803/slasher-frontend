export enum BookType {
  Free = 0,
  OpenLibrary = 1,
  UserDefined = 2,
}

export enum BookActiveStatus {
  Inactive = 0,
  Active = 1,
  Deactivated = 2,
}

export enum BookDeletionState {
  NotDeleted = 0,
  Deleted = 1,
}
