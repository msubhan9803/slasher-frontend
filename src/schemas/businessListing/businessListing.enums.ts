export enum BusinessType {
  VENDOR = 'vendor',
  ARTIST = 'artist',
  BOOKS = 'books',
  MOVIES = 'movies',
  PODCASTER = 'podcaster',
  VIDEO_CREATOR = 'video_creator',
  MUSICIAN = 'musician',
}

export type TrailerLinks = {
  main: string;
  trailer2: string;
  trailer3: string;
};
