export enum FeedCommentType {
  Text = 1,
  Images = 2,
  TextAndImages = 3,
}

export enum FeedCommentStatus {
  Inactive = 0,
  Active = 1,
}

export enum FeedCommentDeletionState {
  NotDeleted = 0,
  Deleted = 1,
}
