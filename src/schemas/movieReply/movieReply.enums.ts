export enum MovieReplyType {
  LocalMovieDb = 0,
  // ExternalMovieDb = 1,
}

export enum MovieReplyStatus {
  Inactive = 0,
  Active = 1,
  // Deactived = 2,
}

export enum MovieReplyDeletionState {
  NotDeleted = 0,
  Deleted = 1,
}
