export enum BookUserStatusFavorites {
  NotFavorite = 0,
  Favorite = 1,
}

export enum BookUserStatusRead {
  NotRead = 0,
  Read = 1,
}

export enum BookUserStatusReadingList {
  NotReadingList = 0,
  ReadingList = 1,
}

export enum BookUserStatusBuy {
  NotBuy = 0,
  Buy = 1,
}

export enum BookUserStatusRatingStatus {
  NotAvailable = 0,
  Available = 1,
}

export enum BookUserStatusStatus {
  InActive = 0,
  Active = 1,
  Deactive = 2,
}

export enum BookUserStatusDeletionStatus {
  NotDeleted = 0,
  Deleted = 1,
}
