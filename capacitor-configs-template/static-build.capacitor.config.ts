import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sdei.slasher',
  appName: 'Slasher',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
