export enum ReportReaction {
  // Note: These 0 and 1 values are the opposite of what the old API comments say, but it seems more
  // correct based on behavior in the admin UI.
  Unreported = 0,
  Reported = 1,
}
