export enum MovieCommentType {
  LocalMovieDb = 0,
  // ExternalMovieDb = 1,
}

export enum MovieCommentStatus {
  Inactive = 0,
  Active = 1,
  // Deactived = 2,
}

export enum MovieCommentDeletionState {
  NotDeleted = 0,
  Deleted = 1,
}
